package self.training.locationpininmap.utils

import com.google.gson.Gson
import java.io.BufferedReader
import java.io.File
import java.util.*
import kotlin.collections.ArrayList

class FromJsonFileToObject {

    /*fun readJsonPostToArray (file: String) : MutableCollection<Objects> {
        var locations = ArrayList<Dictionary<String, Objects>>()
        var gson = Gson()
        val bufferedReader: BufferedReader = File(file).bufferedReader()
        val inputString = bufferedReader.use {
            it.readText()
        }
        var locationPin = gson.fromJson(inputString, LocationList::class.java)

//        locationPin.

        return locations
    }*/
}